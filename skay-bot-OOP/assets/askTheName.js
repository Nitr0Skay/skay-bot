function askName(parentElement) {
    const parentElement = parentElement;
    const formElement = document.createElement('form').classList.add('askName');
    const labelElement = document.createElement('label');
    const labelContent = document.createTextNode('Podaj proszę swoje imię, drogi użytkowniku: ');
    const inputElement = document.createElement('input');
}